import React from 'react';
import ReactTestUtils from 'react-dom/test-utils';
import App from '../../application';
import renderer from 'react-test-renderer';

test('renders an instance of the ContactForm component'), () => {
  
}

test('renders a disabled submit button when no text input exists', () => {

})

test('updates the first name field updates when typed into', () => {

})

test('updates the last name field updates when typed into', () => {

})


test('updates the email field updates when typed into', () => {

})

test('updates the message field updates when typed into', () => {

})

test('checks for proper email format', () => {

})

test('calls the handleSubmit function is called when submit button is clicked'), () =>{

}
